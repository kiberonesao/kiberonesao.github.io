function rb(){
    let northpole = document.getElementsByClassName("input_field")
    for(let i of northpole){
    alert(i.value)
    if(i.value){
        i.style.boxShadow = "none"
    }
    else{
        i.style.boxShadow = "0px 0px 10px red"
    }
}
}

var offcet = 0

function scroll_right(){
    let carousel = document.getElementsByClassName('image_carousel')[0]
    let maxX = -parseFloat(window.getComputedStyle(carousel).width)
    let card_style = window.getComputedStyle(document.getElementsByClassName('image_card')[0])
    if(offcet>maxX){
    offcet-= parseFloat(card_style.width) + 2*parseFloat(card_style.marginRight)
    carousel.style.transform = "translateX("+offcet+"px)"
}
}

function scroll_left(){
    let carousel = document.getElementsByClassName('image_carousel')[0]
    let card_style = window.getComputedStyle(document.getElementsByClassName('image_card')[0])
    if(offcet<0){
    offcet += parseFloat(card_style.width) + 2*parseFloat(card_style.marginRight)
    carousel.style.transform = "translateX("+offcet+"px)"
}
}                                                        































































































































































































































































































































































































































































































